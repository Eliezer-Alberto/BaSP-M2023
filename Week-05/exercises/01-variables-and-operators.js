/* 1. Variables y Operaciones */

/* a. Crear dos variables numéricas y utilizar el operador suma para guardar el valor de la suma
    de ambos números en una 3er variable.*/
    
    var firstNumber = 70;
    var secondNumber = 30;
    var result = (firstNumber + secondNumber); 
    console.log(' The result of this sum is: ' + result);

/* b. Crear dos variables tipo String y concatenarlas guardando el resultado en una 3er variable. */
    
    String = (nameOne, nameTwo);
    var nameOne = 'Eliezer ';
    var nameTwo = 'Alberto ';
    var namesResult = (nameOne + nameTwo);
    console.log('The result of this operation is: ' + namesResult);

/* c. Crear dos variables tipo String y sumar el largo de cada variable (cantidad de 
    letras del string) guardando el resultado de la suma en una 3er variable (utilizar lenght). */

    String = (nameA + nameB);
    var nameA = 'Joaquin';
    var nameB = 'Luciano'; 
    console.log('The length of the variable nameA is: ' + nameA.length);
    console.log('The length of the variable nameB is: ' + nameB.length);
        
    /* Exercise 01 finished */